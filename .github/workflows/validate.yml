name: Empty Workflow

on:
  push:
    branches:
      - main

jobs:
  empty_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Empty step
        run: echo "This is an empty GitHub Actions workflow."